/* const fs = require('fs');
const path = require('path');
const { stdin, stdout } = process;
const readLine = require('readline').createInterface({
  input: stdin,
  output: stdout
});

const pathToFolder = (__dirname);
 */